# 蜂窝（手机卡上网）可行性评估 —— 从"架构级无解"改判为"值得一试"

> 2026-09-25。设备当时在线（up 45 min，load 0.37），本轮取证**全部只读**，未加载任何模块、未改动设备。
>
> 关联：[项目总览.md](项目总览.md) §5 · [硬件现状.md](硬件现状.md) §二 · [WiFi与蜂窝修复方案.md](WiFi与蜂窝修复方案.md) §C

---

## 0 结论先说

| 问题 | 答案 |
|---|---|
| 能不能**研究出确定答案**（为什么复位、能不能修）？ | ✅ **能，高置信度**。几天内可以给出"能"或"确定不能"的结论 |
| 能不能**真的跑通一次手机卡上网**？ | ⚠️ **不确定，估 1/3 ~ 1/2** |
| 能不能**稳定长期可用**？ | ⚠️ 再打折到 **1/5 ~ 1/3** |
| 这是"缺硬件能力"吗？ | ❌ **不是**。这台机器在 Android 下 IPA 数据面**跑通过**，本机日志还在（§2.3） |
| 真正的未知有几个？ | **只有一个**：为什么加载 `ipa.ko` 会整机复位 |

**为什么要改判**：之前文档断言"内置方案基本无解 / 架构级缺失"，依据只有 `/etc/modprobe.d/` 里那句注释
（`# IPA resets this NX595J with the current 6.12 device tree`）加上移植包三处删除 `ipa.ko` 的事实。
本轮补齐了设备侧与上游侧两类证据后，这个结论**过强，必须修正**。

---

## 1 之前的判断错在哪

| 之前的说法 | 现在的事实 |
|---|---|
| "蜂窝数据被 IPA 卡住，**架构级缺失**" | 卡住是真的，但**卡的是软件配置，不是硬件能力** |
| "换 OpenWrt 也没用" | ✅ 这条仍然成立（数据面是同一个内核驱动） |
| "上游定位 Wi-Fi-only，是整体摘除" | ✅ 成立，但**摘除是结果不是取舍**——作者加载过一次、复位了，然后选择了移除，从未定位原因 |
| "IPA probe 就 boot loop" | ⚠️ 措辞不准：**作者从未留下那次复位的任何日志**，boot loop 只是推论（黑名单让模块不会再被加载）。真实情况是"**一次复位，原因未知**" |

---

## 2 设备侧证据（2026-09-25 实测）

### 2.1 内核配置与模块

```
CONFIG_QCOM_IPA=m            ← IPA 驱动是编好的模块
CONFIG_QCOM_MDT_LOADER=y     ← 固件加载器在
CONFIG_QRTR=m / QRTR_SMD=m / RPMSG_QCOM_SMD=y / GLINK=y
CONFIG_RMNET=m
CONFIG_QCOM_Q6V5_MSS=m       ← modem remoteproc 在
```

| 检查项 | 结果 |
|---|---|
| `/lib/modules/6.12.95+/kernel/drivers/net/ipa/` | **空目录** —— `ipa.ko` 被人为删掉了 |
| 删在哪 | 仓库 `kernel/build-kernel.sh:51`、`build-boot.sh:54`、`build-rootfs.sh:46` **三处 `find … -delete`** |
| 还有硬闸门 | `build-rootfs.sh:138` —— 一旦发现 `ipa.ko*` 就 `die "unsafe ipa.ko must not be installed"` |
| `rmnet.ko` | ✅ 在（`drivers/net/ethernet/qualcomm/rmnet/rmnet.ko`） |
| `qmi_helpers.ko` | ✅ 在 |
| 黑名单 | `blacklist ipa` + `install ipa /bin/false`（`modprobe -n -v ipa` 直接返回 `install /bin/false`） |

### 2.2 设备树里的 IPA 节点 —— **接线是完整的，`status = okay`**

`/proc/device-tree/soc@0/ipa@1e40000`（用 Python 解 DTB cells，未依赖 `dtc`）：

| 属性 | 值 | 判断 |
|---|---|---|
| `compatible` | `qcom,msm8998-ipa` | ✅ **主线原生支持**（见 §3.1） |
| `status` | `okay` | ✅ 节点是启用的 |
| `firmware-name` | `qcom/msm8998/nubia/ipa_fws.mbn` | ✅ 文件存在（见 2.3） |
| `reg` | `0x1E40000+0x7000`(ipa-reg) / `0x1E48000+0x2000`(shared) / `0x1E04000+0x2C000`(gsi) | ✅ 与主线写法一致 |
| `iommus` | `→ /soc@0/iommu@16c0000`，streamid **6368 / 6370** | ✅ 与原厂下游 DT 的 6368(ap)/6370(uc) 一致 |
| `interconnects` | 5 个 qnoc：1500000 / 1625000 / 1008000 / 1705000 / 17900000 | ✅ 与 §2.6 的 5 条 `sync_state` 一一对应 |
| `qcom,smem-states` | `→ /smp2p-mpss/ipa-ap-to-modem` | ✅ 正是 IPA 的标准 smp2p 入口 |
| `memory-region` | **phandle 56 → `/reserved-memory/memory@95600000`（`0x95600000`，64 KiB，`no-map`）** | ⚠️ **头号嫌疑，见 §4** |

平台设备 `/sys/bus/platform/devices/1e40000.ipa` 已经注册，`MODALIAS=of:NipaT(null)Cqcom,msm8998-ipa`。

### 2.3 IPA 固件 —— 在，而且两份是同一份

```
/lib/firmware/qcom/msm8998/nubia/ipa_fws.mbn    33088 B   md5 ca39d228b2d5c8217c5f59ea32446597
/lib/firmware/qcom/msm8998/xiaomi/ipa_fws.mbn   33088 B   md5 ca39d228b2d5c8217c5f59ea32446597
```

同一目录还整套躺着 `modem.mdt/modem.b*`、`mba.mbn`、`adsp.*`、`wlanmdsp.mbn` —— 也就是
**IPQ 之外那条链所需的东西都在设备上**，不是"缺固件"。

### 2.4 ⭐ 本机 Android 下 IPA 数据面**确实跑通过**（原始日志）

工作区 `_bootloop/kernel-only.txt` 是本机那次 Android（LineageOS）启动抓下来的内核日志
（同目录 `sess-1-meta.txt`：`sys.boot.reason=reboot,shell`、`uptime=14.10`）。里面：

```
02-04 23:44:13.498  RNDIS_IPA module is loaded.
02-04 23:44:13.548  rmnet_ipa3 started initialization
02-04 23:44:17.032  rmnet_ipa3 started initialization
02-04 23:44:17.032  IPA SSR support = True
02-04 23:44:17.032  IPA ipa-loaduC = True          ← AP 自己加载 IPA 微控制器固件
02-04 23:44:17.032  IPA SG support = True
02-04 23:44:17.032  IPA Napi Enable = False
02-04 23:44:17.032  IPA driver initialization was successful
02-04 23:44:17.047  rmnet_ipa completed initialization
02-04 23:44:23.060  Sending QMI_IPA_INIT_MODEM_DRIVER_REQ_V01
02-04 23:44:23.149  QMI_IPA_INIT_MODEM_DRIVER_REQ_V01 response received   ← 握手成功
```

**这是本机（同一台 NX595J）的 IPA 数据面成功初始化的直接证据**，也说明 modem 侧完全配合。

### 2.5 modem 活着，而且**正在等 AP 侧的 IPA**

`qrtr-lookup` 实测（部分）：

```
  43  v2  Subsystem control service
  21  v1  Modem embedded file system service
4097      DIAG service (MODEM:CMD) / (MODEM:DCI_CMD)
  49  v1  ★ IPA control service
```

- `remoteproc0 = 4080000.remoteproc` → **state=running**（这就是片内 MSS/modem）
- `remoteproc1 = 17300000.remoteproc` → running
- IPA 的 QMI 服务（0x31）**已经出现在总线上** —— 这是"modem 侧就绪、等 AP 侧驱动接洽"的信号

### 2.6 一个指纹：IPA 的 interconnect 消费关系已登记、但永远不会 sync

本次启动 +47.07s 有 5 条：

```
qnoc-msm8998 1500000.interconnect: sync_state() pending due to 1e40000.ipa
（1625000 / 1008000 / 1705000 / 17900000 同样）
```

因为这 5 个 icc provider 在等消费者 `1e40000.ipa` 调 `icc_sync_state()`，而它没有驱动、永远不会调。
**含义**：DTS 里的 icc 接线是**被内核认可**的（否则不会登记消费者）；同时也意味着——
**第一次 probe 成功时，这 5 个 qnoc 会同时丢掉 boot 时的带宽票**，这是首个 probe 值得盯的一个点。

### 2.7 用户态：**0%**，但装起来很容易

| 项 | 状态 |
|---|---|
| `rmtfs` / `tqftpserv` | ✅ **二进制都在**（`/usr/bin/rmtfs`、`/usr/bin/tqftpserv`，包 `rmtfs 1.1-4` 已装），**只是被 systemd mask** |
| `qrtr-lookup` | ✅ 在 |
| `qmicli` / `uqmi` / `mmcli` / `ModemManager` | ❌ **全都没有** |
| `apt-get` | ✅ 可用（设备经 PC 的 ICS 出网） |

### 2.8 ⚠️ 一个必须记住的坑：`grep -i ipa` 会被 SMMU 骗

历史 kmsg 日志里 `grep -il ipa` 命中所有文件，但**全是**这类行：

```
arm-smmu 1680000.iommu:  Stage-1: 36-bit VA -> 36-bit IPA
```

这里的 **IPA = Intermediate Physical Address**，和 IP Accelerator 毫无关系。
精确 grep（`1e40000|ipa_fws|ipa_uc|qmi_ipa`）的结果是：**历史所有启动日志里没有任何 IPA 驱动加载记录** ——
也就意味着**移植作者那次复位没留下任何现场**，这既是坏消息（原因未知），也是好消息（要重新取证，而且不难）。

---

## 3 上游侧证据

### 3.1 主线原生支持 MSM8998 的 IPA

```c
/* drivers/net/ipa/ipa_main.c */
{ .compatible = "qcom,msm8998-ipa", .data = &ipa_data_v3_1 },
```

由 Alex Elder 于 **2021-06 net-next** 合入（`ipa_data-v3.1.c`，533 行）。原始实现来自
**AngeloGioacchino Del Regno（SoMainline）**，并注明 "Jami has been helpful **testing these changes on his hardware**"
—— 即 **真的在有 MSM8998 的机器上跑过**。

### 3.2 但主线**没有** msm8998 的 IPA 设备节点

抓下主线 `arch/arm64/boot/dts/qcom/msm8998.dtsi`（77940 B）逐行确认：

```
94:  ipa_fw_mem: memory@95200000  { reg = <0x0 0x95200000 0x0 0x10000>; no-map; };   ← 64 KiB
99:  ipa_gsi_mem: memory@95210000 { reg = <0x0 0x95210000 0x0 0x5000>;  no-map; };   ← 20 KiB
104: gpu_mem: memory@95600000     { reg = <0x0 0x95600000 0x0 0x100000>; no-map; };  ← 1 MiB
109: wlan_msa_mem: memory@95700000
```

**`grep "ipa@"` 无结果** —— 主线只预留了内存，没有写 IPA 设备节点。
⇒ **我们这个 `ipa@1e40000` 节点是移植作者手写的**（或从下游改写的），**没有任何上游参照物**。
这是本方案最大的变量，也是 §4 那个嫌疑点为什么值得怀疑的原因。

### 3.3 社区状态：MSM8998 的 modem 是 "Partial"

postmarketOS 芯片页（Qualcomm Snapdragon 835 / MSM8998）：

| 组件 | 状态 |
|---|---|
| **Modem（Calls, SMS, Internet）** | **Partial** ← 有人在 MSM8998 上做到了部分 |
| WiFi | **Works** ← 顺带说明：**我们的 WiFi 崩是移植包的 `key_hw_accel` 补丁，不是 SoC 限制** |
| Bluetooth | Works |

对照：SDM845（同为 IPA 平台）的 POCO F1 页面 `Modem: Calls Partial / SMS Partial / **Mobile data Partial**`。
即 **IPA 平台在主线下的移动数据是"能通但需要手工操作"** —— 这正好符合我们的处境。

### 3.4 有现成配方可抄（SDM845 的实操）

postmarketOS 的 Xiaomi Mi A2 Lite 页记录了 IPA 平台起数据面的关键一步：

```bash
qmicli -d qrtr://0 -p --dpm-open-port="hw-data-ep-type=embedded,hw-data-ep-iface-number=1,hw-data-rx-id=4,hw-data-tx-id=5"
killall ModemManager     # 再重启 ModemManager
```

然后 `rmnet_data0` + 一个虚拟接口出现，用 NetworkManager 配 APN 即可。
**参数里的 `rx-id/tx-id` 来自下游内核的 IPA client 枚举** —— MSM8998 的值需要从
`ipa_data-v3.1.c` 的 endpoint 配置反推（这是 D4 的具体活）。

---

## 4 头号未知：为什么整机复位

### 4.1 机制候选（按可疑度排序）

| # | 机制 | 与"无日志的整机复位"是否吻合 | 怎么验证 |
|---|---|---|---|
| **A** | **`memory-region` 指错区**：写固件的目标物理区不是 IPA 的固件区 → TZ 的 XPU 违规 / SMMU fault → **立即拉复位**（这类违规由 TZ 处理，内核来不及打印） | ★★★ 高度吻合 | 见 4.2 对照表；改 DTS 后重试 |
| B | SMMU streamid 不对 → IPA 访问触发 SMMU fault → 同上 | ★★ | streamid 6368/6370 已与原厂下游一致，**可能性低** |
| C | IPA 固件本身与本机不匹配（`ipa_fws.mbn` 是"同一份"从别处拷来的）→ uC assert | ★★ | 与本机 `vendor` 分区里的那份做只读比对 |
| D | probe 成功瞬间那 5 个 qnoc 丢带宽票 → 某个关键消费者欠压/时钟停 → 崩溃 | ★ | 串口日志能看到 icc 相关行 |

### 4.2 `memory-region` 对照表（**最需要盯的一行**）

| 来源 | IPA 固件区 | 备注 |
|---|---|---|
| **主线 msm8998.dtsi** | `0x95200000` + 64 KiB（`ipa_fw_mem`），另有 `0x95210000` + 20 KiB（`ipa_gsi_mem`） | 参考设计布局 |
| **本机原厂（Android）保留内存清单** | `pil_ipa_gpu_region@95200000` = `0x95200000` + **1 MiB** | 原厂把 IPA/GPU 的 PIL 镜像放这里 |
| **本机内嵌移植 DT 实际值** | **`0x95600000` + 64 KiB** ← IPA 的 `memory-region` 指这里 | ⚠️ 主线把 `0x95600000` 叫 **`gpu_mem`** |
| 移植 DT 里其它 0x956xxxxx 区 | `95610000`+20 KiB、`95615000`+1 MiB、`95715000`+1 MiB | 尺寸像"抄了主线 ipa_fw/gsi 的大小，但换了基地址" |

> **读法**：`0x95600000` 这个 64 KiB 区的**尺寸**和主线的 `ipa_fw_mem` 一模一样（64 KiB），
> 但**基地址**在主线里属于 `gpu_mem`。这既可能是"照着原厂内存图摆的"（那就是对的），
> 也可能是"抄了尺寸抄错了地址"（那就是 A 机制，一击致命）。
> **光靠静态对比无法定案** —— 这正是需要上机做 §5 的 G1 的原因。
>
> 顺带：本机 DT 的 `memory@95200000` 是 **1 MiB，与原厂 `pil_ipa_gpu_region` 的 base+size 完全一致**，
> 所以"指向 0x95200000"是一个现成的备选目标。

---

## 5 三道门（这就是"几天"的全部内容）

| 门 | 判据 | 过了说明什么 | 失败的处置 |
|---|---|---|---|
| **G1** | `insmod /root/ipa.ko` 后 **活过 60 秒**（心跳继续 + SSH 仍在） | 复位是"配置不对"而非"硬件不能"；后面纯属软件修 | 复位 → 上 §4.2 的 DTS 修正（**需刷 boot**）再试 |
| **G2** | `/sys/class/net/` 出现 `rmnet_data*` | 数据面真的建起来了 | 查 `qmicli` 的 QMI 返回码 |
| **G3** | 起 PDP 后 `ping` 通（**ping 真实网关，不看接口状态**） | 手机卡上网成功 | 查运营商侧 / APN |

⚠️ **G1 是唯一有"代价"的一步**：失败 = 一次整机复位（约 1~1.5 分钟不可用），不会丢数据、不会变砖。

---

## 6 逐日计划

**D1（今天，零风险，只读）—— 已完成大半 ✅**
本文件即产出。剩余：把 `_bootloop` 里的原厂日志与本次实测做一张比对表；只读挂载核对本机 `vendor`（`sde41`）
里的 `ipa_fws.mbn` 与 `/lib/firmware/` 那份是否同一份（⇒ 判 C 机制）。

**D2（开始动设备，可完全回滚）**
1. 用现成 `kernel/build-kernel.sh` 编出 `ipa.ko`（**必改那三处 `-delete` 与 `die` 闸门**，这是我们 fork 的改动）
2. `modprobe --dump-modversions` 与设备上 `cfg80211.ko` 比 CRC → **上机前就能预判加载是否被拒**
3. **不装进 `/lib/modules`**，只丢到 `/root/`；`insmod /root/ipa.ko`（全路径，绕过 `install ipa /bin/false`）
4. 串口记录器**必须常驻**（`start-serial-log.cmd` 由你双击），`z17s-logwatch` 落盘
5. 抓 **G1** 现场：是"有 dmesg 的 probe 报错"还是"整机复位"

**D3（按 G1 分支）**
- (a) **有报错、设备活着**（最理想）：直接读 dmesg 定位，概率能找到明确修复点
- (b) **复位**：按 §4.2 改 DTS（首选把 `memory-region` 指向 `memory@95200000`）→ 重编 → **刷 boot（含 AVB 签名）** → 再试
- (c) **复位且串口毫无输出**：说明是"TZ/XPU 级"违规 → 回到 (b)，并优先怀疑 A

**D4（G2 / G3）**
`systemctl unmask rmtfs tqftpserv` → `apt install libqmi-utils modemmanager` →
从 `ipa_data-v3.1.c` 反推 `--dpm-open-port` 的 endpoint id → 起 PDP → `ping` 网关。

**D5+** 稳定化：开机自启、自动重连、与 `usb0` 的路由优先级（**别让蜂窝抢掉 usb0 的默认路由，否则会丢管理通道**）。

---

## 7 风险与止损

| 风险 | 等级 | 处置 |
|---|---|---|
| 🔴 **`ipa.ko` 一旦落进 `/lib/modules/…`，开机时 udev 会按 `MODALIAS=of:NipaT(null)Cqcom,msm8998-ipa` 自动加载它** → 若它复位 → **boot loop** | 高 | **只放 `/root/`，只 `insmod` 全路径**；黑名单**全程留着**；到 G2 通过前不写 `/lib/modules` |
| 硬复位（每次约 1~1.5 min 不可用） | 中 | 一次会话 ≤3 次；每次复位后**立刻回到"黑名单 + 无 ipa.ko"的已知好状态** |
| 刷 boot（D3-b） | 中 | 走既有流程：`.config` 逐行 diff 过闸门 → `abootimg -k` → 37MB 读回校验；刷前先 T1 备份 |
| 数据/分区安全 | **低** | 全程不碰 `persist`/`modem`/`dsp`/`vendor`/`system`；只刷 `boot`；`userdata` 只加文件 |
| 最坏情况（改坏 boot） | 低 | TWRP（音量上+电源）刷回原 boot；备份见 [备份与恢复.md](备份与恢复.md) |
| 高负载敏感（历史 §7.3 RCU stall） | 中 | modem 数据面会引入新的中断/NAPI 负载；**即使跑通也要观察稳定性**，别在数据面刚通时叠加高 IO |

**中止条件**（任一命中就收工、回到现状）：
1. G1 连续 3 次复位且串口无任何输出（说明是 TZ 级保护，静态信息不足以定位）
2. 出现任何一次 `persist`/`modem` 相关写入需求
3. 设备出现新的、与蜂窝无关的不稳定（说明触及了 §7.3 那类整机级故障边界）

---

## 8 诚实边界：我做不到 / 不建议做的

- **不可能"几天全自动"跑完**：涉及刷 boot、复位后的物理状态、以及串口记录器必须由你双击启动
  （工具后台进程约 10 分钟会被杀）。**关键几步需要你在场或明确授权。**
- **不保证成功**。§0 给的就是 1/3 ~ 1/2（跑通一次），且**没有上游参照物**是最大的不确定因素。
- **不碰任何危险分区**，也不为这件事去 `modprobe -r ath10k_snoc` 之类"顺手试一下"的操作。
- 就算蜂窝通了，**它也不会替代 `usb0`**：这台机器的管理通道（SSH + 串口 + RNDIS）在那根 USB 线上，
  蜂窝最多是"多一条出网路径"。真正省事的方案仍然是外挂一个几十块的 4G 随身 WiFi —— **成本 1/100、风险 0**。

---

## 9 为什么蜂窝值得试，而 WiFi 不值得

| | **WiFi（WCN3990）** | **蜂窝（IPA / MSS）** |
|---|---|---|
| 根因在哪一层 | **modem Q6 里的 Wi-Fi 固件自己 assert** —— 我们改不了 | **AP 侧的 IPA 驱动 / DTS** —— 我们能改 |
| 上游支持 | 无。主线 6.17 与一加 7T（同款芯片）同样必崩 | **IPA v3.1 已在主线**，且有真机测试记录 |
| 本机存在性证明 | 无 | **有**：本机 Android 下 IPA 数据面初始化成功（§2.4） |
| 失败代价 | MSS 崩溃风暴（负载 1.9、开机活不过 7 分钟），且不可逆 | 一次复位（1~1.5 分钟），可回滚 |
| 结论 | ❌ 已定性，不再投入 | ⚠️ **值得一试** |

> 一句话：**WiFi 的锁在固件里且钥匙不在我们手上；蜂窝的锁在 AP 侧，钥匙就是那份 DTS。**

---

## 10 需要你决定

1. **要不要做 G1**（`insmod ipa.ko`，接受一次可能的复位）？这是整件事的分水岭。
2. 做的话，D2 需要你：**双击 `host/windows/serial/start-serial-log.cmd`** 并**保持窗口开着**。
3. 若 G1 判定要走 D3-b（刷 boot），需要你确认可以接受"刷 boot + AVB 签名"这一步。

> 什么都不做也没关系：现状（`usb0` 有线 + `wifi off`）是稳定的，蜂窝只是"可选项"。
