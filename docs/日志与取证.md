# 日志与取证

> 目标：设备再出事时，**不要再只剩一张手机拍屏的照片**。

## 一、为什么专门做这一层

2026-09-21 设备在跑备份时整机卡死，屏幕和串口一直在刷 RCU stall 报告。
重启后去翻 journald，结果：

```
$ journalctl -b -1 -o short-monotonic | tail -3
[  5783.529396] systemd-timesyncd[285]: Timed out waiting for reply from ...
[  5793.779284] systemd-timesyncd[285]: Timed out waiting for reply from ...
[  5804.029215] systemd-timesyncd[285]: Timed out waiting for reply from ...
$ journalctl -b -1 | grep -c -i "stall"
0
```

**一条 stall 都没有。** 不是 journald 配置错了 —— 之前就已经是
`Storage=persistent`（`/var/log/journal/` 有 machine-id 目录，`--list-boots` 能看到 -4…0）。

真正的原因是物理限制：

> RCU stall / 关中断死等时，内核不再调度用户态进程。
> journald 也是用户态进程 —— 它连 `/dev/kmsg` 都读不了，自然写不进任何东西。

所以设备侧再怎么加脚本，都救不了"整机停摆"这一刻。
**能救的只有一条路：让 PC 一直趴在串口上录。** 因为 console 输出走的是 printk
原子路径，不需要任何用户态参与。

结论沉淀成三条设计约束：

| 约束 | 原因 |
|---|---|
| 取证必须有一端在**设备之外** | 设备停摆时设备内一切都是死的 |
| 设备侧日志要**尽量早落盘** | 只能保证"停摆前最后一刻"，所以延迟越小越好 |
| 必须有**进度心跳** | 否则无法判断"停摆发生在第几秒"，日志断在哪都是个谜 |

## 二、三层日志体系

```
┌─ L1  PC 侧串口持续记录（z17s-serial-log.ps1）   ← 唯一能扛整机停摆
│      PC 一直读 COM15，实时落盘，带 PC 时间戳锚点
│      顺带消除"串口写阻塞拖死 PID1"的老雷
│
├─ L2  设备侧低延迟守护（z17s-logwatch.py）        ← 停摆前 0.25 秒
│      直读 /dev/kmsg，攒够 8KB 或每 0.25s 就 fdatasync
│      每 10s **往串口直写**心跳、每 60s 存现场快照
│
└─ L3  journald 加固（10-z17s.conf）               ← 兜底 + 可检索
       SyncIntervalSec 300s → 5s，放开速率限流
```

三层是互补的，不是替代关系：

- **L1** 拿得到整机停摆现场（RCU stall 全文、各 CPU 栈），但只在 PC 插着线并运行记录器时有效
- **L2** 拿得到停摆前最后一刻，且不依赖 PC；快照还能看出"哪个进程挂在哪个内核函数"
- **L3** 提供 `journalctl` 的检索能力（按单元/优先级/时间查），日常排错最顺手

## 三、L1：PC 侧串口持续记录器

### 启动

双击 `host\windows\serial\start-serial-log.cmd`，窗口留着别关。

或手工：

```powershell
.\host\windows\serial\z17s-serial-log.ps1 -OutDir D:\z17s-log
```

产物在 `_z17s\serial-log\`：

| 文件 | 内容 |
|---|---|
| `serial-YYYYMMDD-HHMMSS.log` | 串口原文，每块数据前有 PC 时间戳锚点 |
| `serial-log.status` | 每 10 秒刷新：端口、累计字节、最后数据/心跳时间、重连次数、`hb_src`、`dev_boot_at`、`last_probe`（最近一块数据的可打印形式，排障用） |

### 特点

- **自动探测 COM 口**：优先取 PnP 里 `VID_0525` 且状态 OK 的那个（当前是 COM15）。
  ⚠️ 注意历史上 COM9/COM11 是 `PID_A4A7` 那套的残留，**别手工指定错**。
- 🔴 **必须排除主板自带的物理串口（`ACPI\PNP0501` 之类，本机是 COM1）**。
  2026-09-21 实际踩到：19:22 手机 USB 掉线后，`SERIALCOMM` 里的 COM1 仍然
  *可正常打开*，于是记录器"重连成功"并**死抱 COM1 不动** —— `serial-log.status`
  显示 `port=COM1 state=idle`，看上去一切正常，实际上一个字节都收不到，
  而且手机以 COM15 回来时它**也不会自动切回去**。
  修法：候选列表里先剔除 ACPI 串口（PnP 查询失败时兜底剔除 COM1）。
  👉 判据：`serial-log.status` 里 `port=` 应该是 **COM15**；若是 COM1，说明记录器读错口了。
- **断线自动重连**：设备重启时 COM 口会消失，脚本会等 6 秒重试，**每次重连都重新扫描**
  （COM 号在重新枚举后通常会变，候选列表算一次就固定是错的）。
- **没插设备时会一直等**，不会退出 —— 记录器一死就没人读设备 console，
  而 console 无人读会让 PID1 写串口阻塞、开机硬卡（见 `修复记录.md` §7.1）。
- **每次写入都 `Flush($true)`**：真落盘，PC 突然断电也保住已收到的。
- **不会误报停摆**：只有在"曾经收到过至少一次心跳"之后才开始检测，
  所以没装 L2 时不会一直告警。
- ⚠️ **心跳识别必须用滚动窗口，不能只看单块数据**：USB CDC/ACM 会分包，
  一行 42 字节的 `z17s-hb ...` 会被切成 `…z` + `17s-hb …` 两块；又因为每行长度固定，
  **切点每次都落在同一个位置** —— 单块匹配就成了 100% 漏检，症状是
  "日志里明明有心跳、status 却一直报 NO HEARTBEAT"（2026-09-21 实际踩到）。
  现在的做法：`$probe = 上一块尾巴(16 字符) + 这一块`，再在里面找 `z17s-hb`。
  排障时看 `serial-log.status` 的 `last_probe` 字段 —— 它会直接显示收到的原始块。
- 🔴 **开端口后收到的第一段是"积压数据"，不能当时间锚点**（2026-09-22 实际踩到）。
  `logwatch` 每 10 秒往 `/dev/ttyGS0` 写一条心跳，而**没人读的时候这些字节不会消失**——
  它们堆在 tty 缓冲里，一旦记录器打开端口就整段涌出来。于是：
  - 记录器会拿**最老那条积压心跳**去算"设备开机时刻"，错得离谱。
    实测：设备真开机 **21:22**，日志第 8 行却写 `device boot @ 22:31:56 (uptime 1871s)`
    —— 因为积压的第一条是 **21:53** 的 `uptime=1871`，差了 **69 分钟**（正好等于记录器空档时长）。
    连带那行的 `gadget rebuild expected @ 22:32:41` 也是废话。
  - 判决办法：开端口后设一个 **排空窗口**（`-DrainSeconds`，默认 4 秒），窗口内收到的心跳标
    `backlog`；此外**一块里挤了 ≥2 条心跳**也算积压（积压是整段涌出来的，新鲜心跳每 10 秒才一条）。
    只有"新鲜"心跳才写入 `device boot @ … (fresh)` 并推开机时刻。
  - 判据：`serial-log.status` 里 **`hb_src=fresh`** 才能信 `dev_boot_at`；`hb_src=backlog`
    说明还没等到一条新鲜心跳，此刻的 `dev_boot_at` 是错的。
  - 日志正文里会插一行 `NOTE everything below until the next 'device boot @ ... (fresh)' line
    is BACKLOG …`，翻旧日志时先找这行。
  - 🎁 顺带的好消息：正因为积压会补发，**晚开记录器不会丢前面的输出**（跟 console 的
    `CON_PRINTBUFFER` 回放是两回事，但效果一样），不必为了抓开机就开始录。

### 让它在开机时自动跑（可选）

不想每次手动双击，把 `.cmd` 的快捷方式丢进启动文件夹：

```
Win+R → shell:startup → 把 start-serial-log.cmd 的快捷方式粘进去
```

## 四、L2：设备侧低延迟守护

### 安装

```bash
# 在 PC 上
tar -cf - device/usr/local/sbin/z17s-logwatch.py \
          device/usr/local/sbin/z17s-screendump \
          device/etc/systemd/system/z17s-logwatch.service \
          device/etc/systemd/journald.conf.d/10-z17s.conf \
          scripts/install-logwatch.sh \
  | ssh z17s 'rm -rf /tmp/z17s-inst && mkdir -p /tmp/z17s-inst && tar -xf - -C /tmp/z17s-inst'

ssh z17s 'bash /tmp/z17s-inst/scripts/install-logwatch.sh /tmp/z17s-inst'
```

脚本幂等，可反复跑。它会剥掉 CR（CRLF 会让 shebang 变成 `python3\r` 而执行失败）、
做 `py_compile` 自检、装文件、重启 journald、启动服务并验证心跳。

### 产物

都在 `/var/log/z17s-kmsg/`：

| 文件 | 说明 |
|---|---|
| `kmsg-<bootid8>-<启动时刻>.log` | 内核日志。单文件 16MB，保留最近 12 个 |
| `snapshot-<bootid8>.log` | 每 60 秒一段现场快照 |
| `boots.tsv` | 每次开机登记一行（boot_id / release / 首次见到时间 / uptime） |
| `latest` | 指向当前日志的符号链接 |

日志行格式是 `<monotonic 秒> <printk 级别> 正文`：

```
[  5804.029215] <4> systemd-timesyncd[285]: Timed out waiting for reply ...
[  9700.954969] <15> z17s-hb uptime=9700 load=0.14/0.13/0.10
```

`<15>` = facility 1(USER) + level 7(DEBUG)。心跳留在 kmsg 里的就是这个级别的**副本** ——
进 journal、进本站日志文件，但**上不了 console**（原因见下面"心跳为什么直写串口"）。

### 现场快照能看出什么

每 60 秒记一次：loadavg、内存关键项、**每个 CPU 的 jiffies 分解**、
**D 状态（不可中断睡眠）任务及其 wchan**、关键中断计数、UDC/USB 状态。

其中 D 状态那段最有用 —— 卡死时能直接看出是谁挂在哪个内核函数上：

```
D-task  : pid=1234   comm=kworker/u16:2    wchan=msm_serial_gen_irq
```

⚠️ 快照本身也依赖用户态调度，**整机停摆时同样会停**。它的价值在于
"半卡死"（部分 CPU 还能跑、I/O 长时间阻塞）那种场景。

### 心跳为什么直写串口，而不是走 printk

**背景**：本机 cmdline 是

```
clk_ignore_unused loglevel=8 console=tty0 console=ttyGS0,115200n8 fbcon=map:0 …
```

`console=tty0` 让**手机屏幕也成了内核 console**：

```
$ cat /proc/consoles
ttyGS0   -W- (EC  p  )  246:0     ← USB 串口
tty0     -WU (E   p  )    4:1     ← 屏幕
```

而 printk 会把同一条消息**广播给所有已注册的 console**。最初心跳用
`<3>`(KERN_ERR) 投 `/dev/kmsg`（为的是穿过 `console_loglevel=4`），结果：

- 串口收到了 ✔
- **屏幕也被每 10 秒刷一行**，把 tty1 上跑着的 `agetty` 登录提示符**永久冲掉** ✘

症状是屏幕永远满屏滚动的 `z17s-hb`、看不到 `login:`，
**极易被误判成"设备卡死"**（2026-09-21 实际发生，用户拍屏来问）。

**现在的做法**：心跳**直写 `/dev/ttyGS0`**，kmsg 侧只留 `<7>`(DEBUG) 副本。

| 路径 | 屏幕 | 串口 | journal / 日志文件 |
|---|---|---|---|
| 旧（printk `<3>`） | ❌ 被刷满 | ✅ | ✅ |
| 新（直写 ttyGS0 + kmsg `<7>`） | ✅ 干净 | ✅ | ✅ |

两个硬性细节：

1. **必须 `O_NONBLOCK`**：PC 侧没人读串口时写缓冲会满，阻塞写会拖死本进程
   （与"串口写阻塞拖死 systemd PID1"是同一类雷）。非阻塞下写不进去就丢弃 —— 心跳丢了无所谓。
2. **直写的心跳行没有 `[ 1234.567890]` 前缀**。这是区分两条路径最快的方法：
   带前缀 = printk 来的；裸行 = 直写。

**顺带的好处**：屏幕从此变成"异常指示灯"。平时只有

```
Debian GNU/Linux 13 nx595j-debian13 tty1
nx595j-debian13 login:
```

一旦屏幕上**突然冒字**，就说明内核在报错（WiFi 崩溃 / RCU stall / `amsdu` 错误）——
比"一直在刷所以看不出异常"强得多。

想退回旧行为（走 printk 上屏）：把 `Z17S_HB_TTY` 置空即可，`HB_LEVEL` 会自动退回 `<3>`。

### 远程看一眼屏幕：z17s-screendump

设备没有键盘，屏幕是直观证据，但平时只能拍照（又糊又慢）。这个工具直读虚拟控制台缓冲：

```bash
ssh z17s 'z17s-screendump'        # tty1，默认最后 40 个非空行
ssh z17s 'z17s-screendump -n 6'   # 只看最后 6 行
ssh z17s 'z17s-screendump -a'     # 全部行，带行号
```

判读：**只有 2~3 行（Debian 横幅 + `login:`）= 屏幕干净**；满屏滚动 = 有东西在写 console。

> 不用键盘也能恢复屏幕：`printf '\033[2J\033[H' > /dev/tty1` 清屏，
> 再 `systemctl restart getty@tty1` 让登录提示符重绘。

## 五、L3：journald 加固

`/etc/systemd/journald.conf.d/10-z17s.conf`：

| 项 | 默认 | 现在 | 理由 |
|---|---|---|---|
| `SyncIntervalSec` | 300s | **5s** | 强制断电最多丢 5 秒，而不是 5 分钟 |
| `SystemMaxUse` | 10% (≈4.5G) | 1G | 别把 45G 根分区吃光 |
| `RateLimitBurst` | 10000 | 100000 | 崩溃风暴时不再静默丢消息 |
| `Storage` | auto | persistent | 显式声明，别依赖 auto 的推断 |

修改后 `systemctl restart systemd-journald` 生效。验证有没有写错键名：

```bash
journalctl -b -u systemd-journald -n 30 --no-pager | grep -iE 'unknown|ignoring|invalid'
```

> 刻意**没有**写 `MaxLevelKmsg` —— 参数名大小写在各 systemd 版本有差异，
> 而它能带来的收益很小（内核消息本来就以 WARNING 以上为主）。
> 宁可少一个优化，也不要冒"journald 起不来 = 完全没有系统日志"的风险。

## 六、出事之后怎么判读

### 第一步：看心跳断在哪

```powershell
# PC 侧，串口日志末尾
Get-Content .\_z17s\serial-log\serial-*.log -Tail 40
```

正常长这样（心跳已改为直写串口，所以**没有内核时间戳前缀**）：

```
[PC 2026-09-21T15:56:23.025 data]
z17s-hb uptime=10175 load=0.10/0.12/0.09

[PC 2026-09-21T15:56:33.052 data]
z17s-hb uptime=10185 load=0.08/0.11/0.09
```

> 如果看到的是 `[ 1234.567890] z17s-hb …`（带内核时间戳），说明这是**改动前**的
> printk 路径留下的，或者是用 `Z17S_HB_TTY=` 退回了旧行为。

判读法：

- **心跳还在** → 设备活着。日志断只是因为没内核消息，不是卡死。
- **心跳停在 `uptime=NNNN`** → 卡死发生在**开机后第 NNNN 秒**。
  拿 `[PC ...]` 锚点换算成墙上时间，就能对上你当时在干什么。
- **心跳停了但后面还有大量 RCU stall / BUG 输出** → 好消息，现场抓到了，往下看。

### 第二步：确认是不是真的停摆

| 现象 | 结论 |
|---|---|
| 心跳停 + 串口还有输出 | 内核活着，只是调度不平衡 → 还能救 |
| 心跳停 + 串口完全静默 + RNDIS 网卡消失 | 整机停摆 → 只能长按电源 15 秒 |
| 心跳停 + 屏幕黑 | 供电/硬件问题，方向完全不同 |

### 第三步：翻设备侧日志

```bash
# 上一次开机的日志（bootid 前半段能从 boots.tsv 找到）
cat /var/log/z17s-kmsg/boots.tsv

# 当前
less /var/log/z17s-kmsg/latest

# 有明确时间窗时用 journald 更顺手
journalctl -b -1 -k --since "12:40" --no-pager
```

## 七、还没做、但值得知道的

真正想抓"整机停摆那一刻的内核栈"，只有让**内核自己**把现场写进一块重启不清的内存。
这需要重编内核并改 DTS 预留内存：

| 选项 | 作用 | 现状 |
|---|---|---|
| `CONFIG_PSTORE` + `ramoops` | 崩死时 dump 到预留内存，重启后 `/sys/fs/pstore` 可读 | ❌ 未开，且需改 DTS |
| `CONFIG_DETECT_HUNG_TASK` | D 状态超时打印调用栈 | ❌ 未开 |
| `CONFIG_SOFTLOCKUP_DETECTOR` | 软死锁自动检测 + 打印 | ❌ 未开 |
| `CONFIG_MAGIC_SYSRQ` | 能用 SysRq 键强制 dump/重启 | ❌ 未开 |

代价：重编内核（WSL 里约 4 分钟）+ 改 DTS 加 `reserved-memory` 节点 + 刷 boot，
且 DTS 改错有不开机风险（可从 TWRP + T1 备份恢复）。

**当前判断：先不上。** 理由是 L1 的串口记录已经能拿到 stall 报告全文
（里面本来就含 CPU 状态和寄存器），而 ramoops 的边际收益主要在"更早、更完整的内核栈"。
等到某次真的卡在"串口也静默"的场景再动它。

## 八、相关文件索引

| 路径 | 说明 |
|---|---|
| `device/usr/local/sbin/z17s-logwatch.py` | L2 守护本体（心跳直写 `/dev/ttyGS0`） |
| `device/usr/local/sbin/z17s-screendump` | 远程 dump 虚拟控制台内容（看屏幕） |
| `device/etc/systemd/system/z17s-logwatch.service` | L2 单元（**注意不挂启动关键路径**） |
| `device/etc/systemd/journald.conf.d/10-z17s.conf` | L3 加固 |
| `host/windows/serial/z17s-serial-log.ps1` | L1 记录器 |
| `host/windows/serial/start-serial-log.cmd` | L1 双击启动 |
| `scripts/install-logwatch.sh` | L2 部署脚本 |
